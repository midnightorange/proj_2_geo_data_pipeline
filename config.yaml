# 管线默认参数配置（CLI 参数优先级更高）
radius: 500          # 核密度带宽 / 缓冲半径（米）
grid_size: 2000      # 无 --zone 时自动网格边长（米）
lq: 1.0              # 区位商阈值
min_samples: 10      # 样本不足判定阈值
coord: wgs84         # 输入坐标类型：wgs84 / gcj02

# 可选：原始类型 -> 功能区大类映射（仅当输入 type 不是功能区大类时启用）
# type_to_function:
#   餐饮: 商业
#   学校: 科教
